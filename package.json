{
  "name": "weathernode",
  "version": "1.0.0",
  "description": "An app that talks to the google maps and darkskies apis to retrieve fancy weather stuff",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.73.0"
  }
}
